# K-HR Guard 진행 현황판

> 갱신: 2026-08-02 (실 배포 완료 — https://k-hr-guard.pages.dev / 백엔드 임베딩 Gemini API로 전환).
> 이 파일이 유일한 진행상황 기준(source of truth). 항목 완료 시 여기 체크만 갱신.

## 0. 인프라/보안
- [x] SQLite 로컬 DB, pytest 인프라, git 통합 저장소
- [x] 단일 사용자 Auth — `APP_PASSWORD` 환경변수 기반 공유 비밀번호 미들웨어(`backend/app/core/auth.py`) + 프론트 로그인 게이트(`AuthGate.tsx`). 환경변수 미설정 시 개발 편의를 위해 인증 비활성(경고 로그만).
- [x] 민감정보 암호화/접근로그 — 파견근로자 DB 자체를 제거해서 저장되는 PII가 없어짐(문제 자체가 사라짐)

## 1. 룰셋 엔진 (`backend/app/engines/rule_checker.py`)
- [x] 파견 2년 D-Day 계산 (저장 없는 1회성 계산기로 전환, `/calculators` 페이지)
- [x] 위장도급 4대 판단기준 스코어링
- [x] 위장프리랜서(3.3%) 판단기준 스코어링
- [x] 주 52시간 체크
- [x] 월 60시간 체크(4대보험 적용제외 기준)
- [x] 감단(감시단속적) 승인 체크
- [x] 퇴직금 계산기
- [x] 연차휴가 계산기
- [x] 연장/야간/휴일근로수당 계산기
- [x] 주휴수당 계산기(제55조, 물류·서비스 단시간직 최빈 문의)
- [x] 포괄임금제 적정성 검토(사무직 — 포괄임금 내 연장수당 vs 실제 필요액 비교)
- [x] 감단직 야간수당 별도지급 안내(`check_supervisory_intermittent_status`에 `night_premium_note` 추가 — 감단승인=연장/휴일수당 면제이지만 야간수당은 계속 적용되는 실무 함정)
- [x] 실업급여(구직급여) 계산기(고용보험법 제40조·제46조·별표1 — 수급요건/소정급여일수/상하한액, 2026년 실제값 웹검색 반영)

## 2. 4대보험/세무 (`backend/app/engines/tax_calculator.py`)
- [x] 4대보험료 계산(연도별 요율 버전관리, 웹검색 실제값 반영)
- [x] 비과세수당 필터(식대/자가운전)
- [x] 중도정산(월 중도 입/퇴사 보험료 일할계산, 근사치+disclaimer)
- [ ] 4대보험 자격취득·상실 "신고" 자체(정부 신고 연동은 범위 밖으로 재확정)
- [ ] 연말정산 전체 계산엔진(국세청 간소화서비스 영역이라 범위 밖 확정, 개념 설명은 RAG로 커버)

## 3. RAG 법령 Q&A (`backend/app/engines/rag/`)
- [x] ChromaDB 로컬 + fastembed 임베딩 + Gemini 생성 + 인용검증
- [x] study_tag(수험 연계 태그)
- [x] 코퍼스 25개 문서(법조문 16 + 대법원/고등법원 판례 8 + 세법 1) — 전화응대 최빈 질문 위주, 원문/판례 딥서치로 검증됨
- [x] **딥서치 판례 고도화(2026-08-01)**: 위장도급(대법원 2008두4367 현대차), 위장프리랜서 근로자성(2004다29736), 통상임금(2023다302838, 2024년 법리변경 반영), 부당해고(91다17931), 평균임금 특별사정(97다5015), 배달라이더 근로자성(서울고법 2026, 최신 하급심), 포괄임금제 유효요건(2002다16958+2018다298904), 대기시간·감시단속적근로자(2016다243085 경비원) 8건 + 소득세법 시행령 제17조의4(식대비과세)
- [x] **직종 매트릭스 판례 커버리지 2차 점검(2026-08-01)**: 사용자가 "업종별 최근 판례 충분한지 재점검" 요청 — 사무직(포괄임금제)·감단직/운전기사(대기시간·감시단속적근로자 해당여부)에 판례가 아예 없던 갭 발견, 딥서치로 2건 보완. 6개 대표질의 전부 top-3 내 정확 매칭 확인(운전기사 대기시간 질의는 키워드 보강 후 1순위로 개선)
- [x] **검색 아키텍처 개선**: 임베딩용 텍스트(키워드+제목, 짧게)와 LLM 컨텍스트용 텍스트(전체 본문)를 분리 - 실측 결과 fastembed 다국어 모델이 법률 전문용어 간 미묘한 차이를 잘 구분 못해서 위장도급 판례가 검색 3순위 밖으로 밀리는 문제 발견, 분리 후 6개 대표질의 전부 1순위 정확매칭으로 개선. top_k도 3→5로 확대(코퍼스 확대 대비)
- [x] **Gemini 429(할당량초과) 에러 핸들링**: 이전엔 500으로 크래시했음 - `GeminiQuotaExceededError`로 잡아서 429+명확한 메시지 반환하도록 수정, 프론트도 대응 메시지 추가
- [x] 자주 묻는 질문(FAQ) 퀵카드 13개 — 클릭 한 번으로 즉시 질문
- [x] Q&A 대화 히스토리 브라우저 저장(localStorage) + 사례노트 저장 버튼
- [ ] 직무발명보상(발명진흥법 제15조)·선택적근로시간제(근로기준법 제52조)·연차사용촉진 조문 — 매트릭스에서 식별됐으나 아직 미반영
- [ ] 계산 결과 문서화(퇴직금 계산서 등 텍스트 출력) — 계산기 화면에 결과는 나오나 별도 문서 export는 아직
- [ ] **Gemini 무료 티어 하루 20회 한도 실측 확인(2026-08-01)** — `gemini-3.5-flash` free tier 기준. 전화응대 도구로 실사용시 하루 20회 넘으면 막힘, 필요시 유료 전환 검토

## 4. 파견근로자 관리 (방향 전환 완료)
- [x] DB/CRUD/엑셀 업로드/대시보드 완전 제거 (회사 자체 관리프로그램과 중복)
- [x] D-Day/위장도급/위장프리랜서 판정을 저장 없는 1회성 계산기로 `/calculators` 페이지에 통합

## 5. 수험 학습
- [x] SM-2 간격반복 복습
- [x] 사례노트 아카이브 + 검색
- [x] **매일 기출문제 3개(`/exam`, 2026-08-05)** — 2025년 제34회 1차시험 Q-net 공식 문제·정답(공공누리) `app/data/exam_questions.json`. 기존 SM-2 엔진(`spaced_repetition.py`) 재사용해 오늘 복습예정 우선→신규(중요도순)로 3문제 선정, 객관식 자동채점 후 정답/오답을 SM-2 quality(5/1)로 자동 변환. `ExamQuestionProgress` 모델, `exam_daily.py` 엔진, `/exam/*` API, 프론트 `/exam` 페이지, 테스트 8개.
  - ⚠️ **1차 작성 시 노동법I/II·사회보험법 78문항 중 31개(약 40%)가 Q-net 최종정답표와 다르게 오기재됨** — 프론트 검증 중 발견, 즉시 원문 재대조로 전수 수정. 오답 기반으로 써뒀던 해설 9개도 함께 삭제 후 재작성. **기출문제/정답처럼 정확성이 생명인 콘텐츠는 작성 직후 반드시 원문과 1:1 재대조할 것.**
  - [x] **민법 40 + 경제학원론 25(개념형) 추가(2026-08-05)** — 총 143문항. 이번엔 정답 번호를 사람이 직접 타이핑하지 않고 검증된 정답표에서 코드로 자동 매칭해 오기재 자체를 원천 차단.
  - ⚠️ **경제학원론 계산형 문항 15개, 처음엔 "문제 전사 오류로 추정"하고 통째로 제외**(81,83,89,91,96,98,99,102,108,109,111,113,114,115,118번) — 직접 재계산한 답이 "공식 정답"과 계속 어긋나 문제 텍스트 자체가 잘못 옮겨 적힌 걸로 오판했었음.
  - [x] **경영학개론 40 추가(2026-08-05)** — 별도 PDF(2교시 경영학 booklet)를 새로 다운로드해서 확보. 계산형 6문항(104·105·108·109·113·117번)은 전부 직접 재계산해 공식 정답과 일치 확인 후 포함.
  - ⚠️⚠️ **위 "제외" 판단이 틀렸었음을 재검증 중 발견(2026-08-05, 같은 날 두 번째 사고)** — 원인은 Q-net 정답표 PDF에 경영학개론(81-120)과 경제학원론(81-120) 두 과목이 번호가 겹친 채 나란히 인쇄돼 있는데, 경제학원론 문항의 정답을 옆 칸 경영학개론 열에서 잘못 대조한 것. 문제 텍스트는 처음부터 전부 정확했고 정답 대조만 틀렸던 것 — 계산형 문항을 직접 풀어 검증하는 과정에서 "내 계산≠공식정답"이 나오자 곧장 "문제 전사가 틀렸다"고 결론 내린 게 성급했음. 이 사고로 경제학원론 15문항이 부당하게 제외돼 있었을 뿐 아니라, 이미 배포됐던 경제학원론 2문항(116·120)도 오답이 반영된 채 실사용자에게 노출돼 있었음. **교훈: 계산이 안 맞을 때는 "문제가 틀렸다"고 바로 결론짓지 말고, 정답표 자체를 다시 정밀 대조(이번처럼 여러 과목이 같은 번호로 나란히 인쇄된 표는 특히 주의)하는 것을 먼저 확인할 것. 다과목 병기 정답표는 과목명을 key로 갖는 딕셔너리로 관리해 이런 열 착오를 원천 차단할 것.**
  - [x] **경제학원론 15문항 전부 복원 + 기존 오답 2개(116·120) 수정 완료(2026-08-05)** — 정답표 이미지를 PyMuPDF로 3배 확대 렌더링해 육안 재대조, 총 198문항(전 과목 완비: 노동법I 40·노동법II 7·사회보험법 31·민법 40·경제학원론 40·경영학개론 40).
  - [x] **해설(explanation) 198개 전부 작성 완료(2026-08-05)**
  - [x] 배포 완료(Render+Cloudflare) — 1차분(78)→2차분(143)→3차분(183)→4차분(198, 경제학원론 정답 수정판) 순으로 배포·확인
- [ ] (보류) 마이크로 퀴즈(객관식) 형식 — 우선순위 낮음

## 6. 프론트엔드
- [x] shadcn/ui(Base UI) 적용
- [x] 홈 = "전화응대 빠른답변 허브"(법령Q&A/계산기/4대보험 바로가기 + 오늘 복습항목)
- [x] `/calculators` 페이지 — 계산기 12종 카드형 통합
- [x] FAQ 퀵카드 13개
- [x] 로그인 게이트(AuthGate)
- [x] **홈 화면 감정 체크인 + 오늘의 성취 카드(2026-08-05)** — 무거운 상담 업무 사이 잠깐 숨 돌리라고 추가. 이모지 5단계로 하루 기분 1탭 기록(하루 1개, 재클릭 시 덮어씀), 날짜 기준 로테이션되는 위로 문구, 오늘 처리한 상담 케이스 수·새로 학습한 기출문제 수·연속 기록일수를 "오늘의 작은 성취"로 표시. 새 백엔드 로직 없이 기존 `CaseNote`/`ExamQuestionProgress`의 `created_at`만 재사용해서 구현 부담을 최소화(`MoodCheckin` 모델만 신규 추가). `app/engines/wellbeing.py`, `/wellbeing/*` API, 프론트 `WellbeingCard.tsx`, 테스트 8개. 이후 성취 0개일 때 섹션 자체가 안 보이던 것 발견해 안내문구로 수정.
- [x] **이력서 즉시 추출 유틸리티(`/resume`, 2026-08-05)** — 이력서 사진/스캔본(JPG·PNG·WEBP·PDF)을 올리면 Gemini Vision이 이름·연락처·경력·학력·자격증·희망근무조건 등을 구조화해서 보여줌. 계산기와 같은 "즉시 처리, 저장 안 함" 원칙 — 후보자 DB를 새로 만들면 회사 채용 플랫폼과 중복되므로(예전에 파견근로자 명단 DB를 들어냈던 것과 같은 이유) 화면 표시+복사(`전체 복사` 버튼)까지만 하고 영구저장은 안 함. 주민등록번호는 프롬프트에서 명시적으로 절대 추출 금지 처리. 합성 이력서 이미지로 실제 Gemini 호출까지 검증(경력 2건 합산 총 경력연차 자동계산 정확히 확인). `app/engines/resume_extract.py`, `/resume/extract` API(멀티파트 업로드, `python-multipart` 의존성 추가), 프론트 `resume/page.tsx`, 테스트 6개. ⚠️법령 Q&A와 Gemini 무료티어 하루 호출한도(20회)를 공유함.
  - [x] **다중 업로드(최대 5장) 지원(2026-08-05)** — 백엔드 변경 없이 프론트에서 파일마다 순차적으로 `/resume/extract` 호출, 파일별 결과 카드를 따로 표시. 5장 넘게 선택하면 앞 5장만 자르고 경고 문구 표시. 배치 도중 429(한도초과) 뜨면 남은 파일은 즉시 "한도 초과로 건너뜀" 처리하고 낭비 호출 안 함.
  - [x] **고객사 직무조건(JD) 매칭 채점 추가(2026-08-05)** — JD를 입력하면 이력서 추출과 같은 Gemini 호출 안에서 매칭점수(0~100)·강점·우려사항까지 함께 받아옴(추출+채점을 호출 2번이 아니라 1번으로 합쳐서 하루 20회 한도를 그대로 유지). 배치 업로드 시 채점 완료 후 점수 높은 순 자동 정렬. 실제 Gemini 호출로 변별력 검증(딱 맞는 JD엔 100점+구체적 강점 3개, 완전히 다른 직무 JD엔 5점+구체적 사유 — 항상 고득점을 주는 게 아니라 실제로 갈림을 확인). Word(.docx)는 스캔본이 아니라 텍스트문서라 별도 추출 로직이 필요해 이번 범위에서는 제외.
- [x] **코드 재점검·모듈화(2026-08-20)** — 감정체크인·이력서추출 추가하며 생긴 중복 정리.
  - 백엔드: `generation.py`(법령Q&A)·`resume_extract.py`(이력서)·`embeddings.py`가 각자 Gemini 클라이언트 생성, 429/미설정 예외, JSON 파싱을 따로 구현하고 있던 걸 `app/engines/gemini_client.py`로 통합(`get_client()`, `generate_structured_json()`, 공용 예외 2종). 모델을 다시 바꿔야 할 때(과거 gemini-2.5→3.5 전환 같은 경우) 한 곳만 고치면 됨. generation.py 92→47줄, resume_extract.py 163→129줄로 축소. 실제 Gemini 호출로 두 경로(법령Q&A 검색+생성, 이력서추출) 전부 재검증 완료, 129개 테스트 전부 통과.
  - 프론트: `resume/page.tsx`(375줄, 상태관리+로직+렌더링이 한 파일에 뒤섞여 있었음)를 `lib/resume.ts`(타입+순수함수)·`components/resume/ScoreBadge.tsx`·`components/resume/ResumeResultCard.tsx`로 분리, `page.tsx`는 상태관리+오케스트레이션만 남기고 140줄로 축소. 빌드·렌더링 결과 변화 없음 확인.
- [x] **클라이언트 맞춤형 멘트 메이커(`/messages`, 2026-08-20)** — 상황을 짧게 적으면(선택적으로 "면접 확정/계약 갱신/출근 안내/기타" 유형 칩과 함께) 고객사용 격식 이메일·근로자용 카카오톡/문자 안내·면접관용 요약 메모 3종을 한 번의 Gemini 호출로 동시 생성. 문서생성/계약조회(보류)와 달리 이건 "정답이 정해진 일"이 아니라 톤을 상황에 맞게 바꾸는 글쓰기라 LLM이 적합하다고 판단해 진행. **실제 발송 기능은 없음** — 초안만 만들고 복사해서 직접 검토 후 보내는 방식(카카오톡 비즈니스 API 등 실발신 연동은 회사 승인·비용이 드는 별개 프로젝트라 범위에서 제외). "내일", "다음주" 같은 상대적 날짜 표현은 오늘 날짜를 프롬프트에 넣어 실제 날짜(YYYY-MM-DD, 요일)로 환산하도록 처리 — 실제 호출로 "내일 오후 2시"가 정확한 날짜+요일로 변환되는 것 확인. 없는 정보(담당자명, 연락처 등)는 추측 대신 대괄호 placeholder로 남기도록 프롬프트에 명시. `app/engines/message_draft.py`(방금 만든 `gemini_client.py` 재사용), `/messages/draft` API, 프론트 `messages/page.tsx` + `components/CopyableCard.tsx`, 테스트 7개.
  - [x] **유형별 예시 초안 추가(2026-08-20)** — 상황 유형 칩(면접확정/계약갱신/출근안내/기타)을 클릭하면 실제 생성 전에도 하드코딩된 예시 초안 3종을 바로 보여줌(Gemini 호출 없음, 쿼터 무소모). "(예시)"로 명확히 라벨링해서 실제 생성 결과와 혼동 안 되게 처리, 실제로 "초안 만들기"를 누르면 예시 대신 진짜 결과로 교체됨. 상황 설명 입력창의 placeholder도 선택된 유형에 맞는 예시로 바뀜. `lib/messageDraftExamples.ts`.
- [x] **업무 로그 · 보고서(`/worklog`, 2026-08-20)** — 그날그날 대충 적어둔 메모(예: "A사 이력서 5개 전달, B사 근로자 면담 완료")를 저장해두면, "이번 주/이번 달 보고서 만들기" 버튼을 누를 때만 LLM이 모아서 격식 있는 보고서로 정리. **이번엔 처음으로 진짜 영구 저장이 필요한 기능** — 다만 "파견근로자 명단"처럼 회사 시스템과 겹치는 게 아니라 순수 개인 업무일지라 문제없음(이미 있는 `사례노트`와 같은 성격). Render 무료플랜은 재배포마다 SQLite가 초기화되는데, 실적 기록이 통으로 날아가면 손해가 크므로 AI 없이 원본 그대로 뽑는 "전체 내보내기(백업)" 버튼을 같이 제공 — 유료 플랜 업그레이드 없이 "수시 백업"으로 리스크를 상쇄하기로 사용자와 합의. 메모 저장·조회·내보내기는 결정론적 로직(AI 미사용), 보고서 생성만 Gemini 호출. 실제 호출로 두 항목의 메모가 회사별로 묶여 자연스러운 문장의 보고서로 합성되는 것, 주간 범위(월~일)가 정확히 계산되는 것 확인. `app/models/work_log_entry.py`, `app/engines/work_log.py`, `/work-log/*` API, 프론트 `worklog/page.tsx`, 테스트 11개.
- [x] **컴플라이언스 진단(`/compliance`, 2026-08-20)** — "신규 고객사 영업 아이디어 4개" 검토 후 사용자가 선택한 것부터 진행. (잠재)고객사의 현재 인력 운영 방식을 적으면, 법령Q&A와 같은 코퍼스(ChromaDB, 파견법 조문+판례)를 검색해서 위장도급·불법파견 리스크를 진단하고 우리 회사 도급모델 전환 시 이점을 정리한 영업용 리포트를 생성. 법령Q&A의 RAG 파이프라인(검색+인용검증)을 그대로 재사용하되, 스키마가 달라서(진단+영업피치 vs 단순 Q&A) `generation.py`는 안 쓰고 `gemini_client.generate_structured_json`을 직접 호출·`citation_verifier`만 재사용 — 검색 안 된 조문을 LLM이 지어내지 못하게 막음. "실제 법적 자문 아님, 전문가 확인 필요" 문구를 매번 답변 끝에 명시하도록 프롬프트에 강제(법령Q&A의 "행정해석 확인 필요" 패턴과 같은 취지). 실제 Gemini 호출로 검증(3년 파견 초과 사용 시나리오 입력 → 정확히 제6조·제6조의2 인용, 고령자 예외는 정보 부족으로 단정 안 함, 리스크 "높음" 정확히 판정). `app/engines/compliance_check.py`, `/compliance/check` API, 프론트 `compliance/page.tsx`, 테스트 5개.
- [x] **민원 방어(`/complaint-defense`, 2026-08-20)** — "필드 고충 정조준 아이디어 4개"("노무사 질러!"/"CRM 위스퍼러"/"이탈 감지 레이더"/"대타 찾기 시뮬레이터") 검토 후, 리스크가 가장 낮고 즉시 체감 효과가 큰 1번부터 진행(2번은 후순위, 3번은 정량 스코어링의 허위 정밀도·감시 소지 문제로 재설계 필요, 4번은 인력 이력 DB 부재로 보류 — 사용자에게 검토 의견 전달 후 "추천대로 진행해줘"로 확정). 근로자가 보낸 감정적/공격적 민원 텍스트를 그대로 입력하면, 컴플라이언스 진단과 동일한 RAG 코퍼스를 검색해 감정을 배제한 사무적 답변 초안 + 근거 조항(인용검증 통과분만) + 매니저 내부 유의사항(녹취·노동청 진정 대비, 전문가 확인 권고)을 생성. `compliance_check.py`와 거의 동일한 구조를 그대로 복제(스키마만 다름) — 검색 안 된 조문은 지어내지 않고, 회사측 조치 자체가 위법 소지가 있을 때도 일방적으로 회사 편을 들지 않고 "단정하기 어려움"으로 정직하게 hedging하도록 시스템 프롬프트에 명시. 실제 Gemini 호출로 로컬 curl 검증 + 브라우저 UI 클릭스루 검증 모두 통과(무단결근 연차차감 항의 시나리오 → 근로기준법 제60조·제61조 정확히 인용, 회사의 일방적 연차 대체가 위법 소지 있음을 스스로 지적하며 과장 없이 정리). `app/engines/complaint_defense.py`, `/complaint-defense/generate` API, 프론트 `complaint-defense/page.tsx`, 테스트 5개(백엔드 전체 157/157 통과). 프로덕션은 APP_PASSWORD 미보유로 최종 인증 호출은 생략, 로컬 검증으로 충분하다고 사용자 확인.
- [x] **고객사 메일 도우미(`/client-negotiation`, 2026-08-20)** — 같은 아이디어 목록의 2번("CRM 위스퍼러"). 원안은 "심리적 프로파일링"을 표방했으나, 몇 줄 메모만으로 확정적 성향 진단을 내놓는 건 근거 없는 확신을 주는 위험한 설계라고 판단(검토 시 사용자에게 전달, "추천대로 진행해줘"로 전체 순서 확정) — approach_notes는 항상 "메모하신 내용으로 보아"처럼 입력 근거를 밝히며 제안하는 형태로 한정하고, 메모에 없는 성향을 단정하지 않도록 프롬프트에 강제. 고객사 담당자 관련 메모 + 이번 메일 목적을 입력하면 접근 방식 제안 + 메일 초안 + 핵심 포인트를 생성. RAG 불필요(법적 근거 인용이 아니라 순수 글쓰기)라 `message_draft.py`와 동일한 구조(순수 `generate_structured_json` 호출) 재사용. 메모/목적에 없는 구체적 정보(담당자명, 금액, 날짜 등)는 대괄호로 표시하도록 강제. 실제 Gemini 호출로 로컬 curl 검증 + 브라우저 UI 클릭스루 검증 모두 통과(숫자중심·지연이슈로예민한 담당자 메모 입력 → 두괄식·정량지표 중심 톤 제안, 이메일 본문에 실제 근거 없는 수치는 전부 대괄호 처리). `app/engines/client_negotiation.py`, `/client-negotiation/draft` API, 프론트 `client-negotiation/page.tsx`, 테스트 6개(백엔드 전체 163/163 통과).
- [x] **이탈 신호 노트(`/attrition-signal`, 2026-08-20)** — 같은 목록의 3번("이탈 감지 레이더"). 원안의 "이탈 위험도 85%" 같은 정량 스코어링은 예측 타당성이 검증되지 않은 숫자가 확신을 주는 위험한 설계이자 근로자 감시 소지가 있다고 판단해, 검토 시 "점수화는 빼고 텍스트로만 요약하는 순한 버전"을 제안 → 사용자가 "3번, 4번 순서대로 진행하자"로 승인. 근로자와의 최근 대화/관찰 메모를 입력하면 관찰된 신호(입력 텍스트에 실제로 존재하는 근거에서만 추출, 확률·점수·확정적 예측 절대 금지) + 접근 제안 + 면담 대화 포인트를 생성하고, 매번 "예단이나 인사상 불이익 조치의 근거로 쓰면 안 된다"는 유의사항을 강제로 포함. RAG 불필요, `message_draft.py`/`client_negotiation.py`와 같은 순수 생성형 구조 재사용. 실제 Gemini 호출로 로컬 curl 검증 + 브라우저 UI 클릭스루 검증 모두 통과(답장 지연·급여 문의 반복·타현장 문의라는 3가지 실제 입력 사실만 신호로 추출, 숫자·확률 없음, 유의사항 정확히 포함). `app/engines/attrition_signal.py`, `/attrition-signal/analyze` API, 프론트 `attrition-signal/page.tsx`, 테스트 6개(백엔드 전체 168/168 통과).
- [x] **AI 어시스턴트(`/assistant`, 2026-08-20)** — "시스템을 사용하기 쉽게 자동화/에이전트화할 방법"을 찾아달라는 요청에 3가지를 제안(텔레그램 봇 게이트웨이/통합 입력창 라우터/업무로그 자동 발송) → 1번(텔레그램)은 "내가 사용자가 아니므로" 보류, 2번(통합 라우터)으로 진행 확정. 4개의 개별 페이지(민원 방어/컴플라이언스 진단/이탈 신호 노트/멘트 메이커)에 매번 사용자가 직접 들어가야 하는 마찰을 없애기 위해, 텍스트를 붙여넣기만 하면 Gemini가 어떤 도구가 필요한지 분류하고 해당 엔진으로 라우팅. 고객사 메일 도우미(`client_negotiation`)는 "메일 목적"이라는 별도 필수 입력이 있어 자동분류 대상에서 제외하고 기존 페이지 유지(무리하게 라우터에 욱여넣지 않음). 분류 1회 + 실제 생성 1회로 하루 Gemini 호출 한도를 개별 페이지보다 빨리 소모한다는 트레이드오프가 있음을 인지. 각 엔진의 기존 최상위 함수(`complaint_defense.generate_defense` 등)를 그대로 재사용해 RAG/프롬프트 로직 중복 없이 라우터 계층만 얇게 추가. 프론트는 결과 타입을 category로 판별하는 TypeScript discriminated union(`AssistantRouteResult`)으로 표현해 4가지 결과 UI를 안전하게 분기 렌더링. 실제 Gemini 호출로 로컬 curl 검증(민원 텍스트 → complaint_defense로 정확히 분류) + 브라우저 UI 검증(면접 일정 안내 텍스트 → message_draft로 정확히 분류, 3종 초안 정상 렌더링) 모두 통과, 콘솔 에러 없음. `app/engines/assistant_router.py`, `/assistant/route` API, 프론트 `assistant/page.tsx`, 테스트 7개(백엔드 전체 175/175 통과).
- [x] **디자인 리프레시(2026-08-20)** — 20대 중반 여성 취향 + 눈피로 최소화 요청. 색상은 순백/순검정 대신 라벤더 틴트를 섞은 파스텔 팔레트(oklch, 로즈핑크 primary/라벤더 secondary/민트 accent)로 `globals.css` 전면 교체, 다크모드 토큰도 함께 정리(순검정 대신 다크 플럼톤 — 대비가 너무 강한 것도 눈피로 원인이라 전경색도 살짝 낮춤). 짱구 캐릭터 요청은 실제로 저작권 있는 캐릭터를 그려 넣는 문제(이 앱이 Cloudflare Pages 공개 URL로 떠 있어 "완전한 사적 이용"이라 보기 어려움)라고 설명 후, "느낌만 가져온 오리지널 마스코트"로 방향 확정 — 짱구st 짙은 눈썹·장난기는 유지하되 스파이크 머리·빨강/노랑 배색 등 식별 가능한 디자인 요소는 전혀 쓰지 않고, 전화응대 헬프데스크라는 앱 정체성에 맞춰 헤드셋을 씌운 완전히 새 캐릭터로 제작(`components/Mascot.tsx`, 순수 SVG). 네비바(26px)·홈 화면(56px)에 배치. 이어서 사용자가 참고 이미지(민트 배경 + 빨강 세모/노랑 네모/초록 동그라미 컨페티 패턴 - 실제로는 짱구 이불 패턴)를 보내며 "이 컬러와 도형을 배경에 넣어달라" 요청 → 기본 도형(세모/네모/동그라미)은 저작권 보호 대상이 아니므로 그 부분만 안전하게 재사용, 사진을 그대로 트레이싱하지 않고 같은 팔레트로 오리지널 컨페티 SVG 타일을 새로 제작해 `body` 배경에 저반복 적용(`--background`도 라벤더에서 민트 틴트로 전환). 카드 콘텐츠 영역은 카드 배경색이 덮어서 여백에서만 은은하게 보이므로, 눈피로 요구사항과 화려한 배경 요구사항이 자연스럽게 양립. 브라우저로 실제 렌더링 확인(홈/업무로그/컴플라이언스/AI어시스턴트/계산기, 밀도 높은 페이지에서도 텍스트 가독성 이상 없음), 콘솔 에러 없음.
- [x] **업무 로그 주간보고서 자동 생성(2026-08-20)** — 같은 자동화 아이디어 목록의 3번. "생성만 자동화, 전달은 수동 확인"으로 사용자가 확정(텔레그램 발송은 1번과 같은 이유로 보류, 프로덕션 API 인증 문제도 회피). Render 무료플랜엔 진짜 크론이 없어("15분 미사용 시 슬립") 정시 자동 실행 자체가 불가능하다는 걸 인지하고, 대신 "금요일 이후 첫 방문"을 트리거로 삼는 지연생성(lazy) 방식으로 설계 — 사용자가 평일 내내 이 앱을 실제로 열어보는 사용 패턴에 맞춘 것. `WorkLogWeeklyReport` 테이블에 주(week_start) 단위로 한 번만 저장하고, 같은 주 재방문 시 재호출 없이 기존 걸 반환(AI 호출 낭비 방지) — 기존 "이번 주 보고서 만들기" 버튼(즉석 생성, 저장 안 함)은 그대로 유지, 완전히 별개 경로. 실제 Gemini 호출로 로컬 통합 스크립트 검증 중 **버그 발견**: Gemini가 가끔 JSON 문자열 안 줄바꿈을 이중 이스케이프해서 파싱 후에도 실제 줄바꿈이 아니라 문자 그대로 "\n" 두 글자가 남는 현상 확인 → `gemini_client.generate_structured_json`에 정규화 로직 추가(모든 문자열 값에서 `\n` 재귀 치환, 이 프로젝트의 모든 AI 기능이 공유하는 공통 계층이라 한 곳만 고치면 전체가 수혜). 재검증으로 실제 줄바꿈 정상 렌더링 확인, 브라우저 UI에서 히스토리 목록·자동생성 안내 문구·복사 버튼 모두 정상. `app/models/work_log_weekly_report.py`, `work_log.ensure_weekly_report`/`list_weekly_reports`, `/work-log/weekly-report/auto`·`/history` API, 프론트 `worklog/page.tsx` 히스토리 섹션 추가, 테스트 9개(gemini_client 정규화 테스트 3개 포함, 백엔드 전체 185/185 통과).

## 6.5 프로덕션 장애 대응
- [x] **로그인 불가 사고(2026-08-20)** — 사용자가 "비번 입력 후 로그인이 안 돼" 보고. 원인: `frontend/.env.local`에 로컬 개발용 `NEXT_PUBLIC_API_BASE_URL=http://localhost:8010`이 남아있었는데, Next.js는 `.env.local`을 `.env.production`보다 우선시켜서 이날 실행한 모든 `npm run build`(컴플라이언스 진단부터 디자인 리프레시까지 총 7회 배포)가 전부 localhost를 가리키는 프론트를 배포하고 있었음 — `.env.production` 파일 자체가 아예 없었던 것도 원인. AuthGate가 `/brief/morning`을 호출해 인증 여부를 확인하는데, 이 요청이 사용자 브라우저에서 존재하지 않는 로컬 서버로 나가니 항상 실패 → 로그인 불가. 근본 수정: `.env.local`을 `.env.development`로 이름 변경(이제 `next dev`에서만 로드, `next build`엔 영향 없음), `.env.production`을 실제 Render URL로 새로 생성 — 이제 매번 수동으로 신경 쓰지 않아도 프로덕션 빌드가 항상 올바른 백엔드를 가리킴. 재빌드 후 번들에 `k-hr-backend.onrender.com`만 있고 `localhost`는 전혀 없음을 grep으로 확인, 재배포 후 실제 프로덕션 URL에서 틀린 비밀번호 입력 시 "비밀번호가 올바르지 않습니다"가 정상 표시되는 것으로 실제 백엔드 연결 확인(브라우저 캐시가 이전 빌드를 물고 있어서 첫 테스트는 재현 실패 → 캐시 삭제 후 재확인). `DEPLOY.md`에 재발 방지 경고 추가, `.env.local.example` → `.env.development.example`로 함께 이름 변경.

## 7. 문서
- [x] `K_HR_Platform_Design_and_Spec.md` 0~2번 섹션 반영
- [ ] 3번 섹션(개발/배포 워크플로우) — 로컬우선 방식과 여전히 불일치, 다음 세션에 동기화
- [x] 메모리(`project_k_hr_platform.md`) 최신화
- [x] git 저장소 통합 + 커밋

## 9. 배포 (2026-08-01 착수, 2026-08-02 완료)
- [x] 프론트: Next.js 정적 export 설정(`output: "export"`), 로컬 빌드 확인
- [x] 백엔드: Dockerfile + render.yaml(Render Blueprint) + .dockerignore
- [x] DB_PATH/CHROMA_DB_DIR, ALLOWED_ORIGINS 환경변수화(배포 유연성)
- [x] `DEPLOY.md` 단계별 가이드 작성
- [x] **백엔드 실제 배포 완료** — https://k-hr-backend.onrender.com (Render, GitHub `irions68-dev/k-hr-platform-` main 브랜치 Blueprint 연동, 자동 재배포)
- [x] **프론트 실제 배포 완료** — https://k-hr-guard.pages.dev (Cloudflare Pages)
- [x] CORS(`ALLOWED_ORIGINS`)에 실제 Pages 도메인 연결 확인
- [x] **임베딩을 fastembed(로컬)→Gemini API로 교체(2026-08-02)** — Render 무료플랜 RAM 512MB에서 fastembed 다국어 모델(~220MB) 로딩 중 OOM으로 프로세스가 죽는 걸 실측 확인, `app/engines/rag/embeddings.py`를 `gemini-embedding-001` 호출로 전면 교체. 검색 품질 저하 없음(위장도급 질의 재검증 시 1순위 정확매칭 유지). `requirements.txt`에서 fastembed 제거, Dockerfile 빌드 가벼워짐.
- [x] **재시작 시 자동 코퍼스 재적재(2026-08-02)** — Render 무료플랜은 재배포/재시작마다 디스크가 초기화돼서 ChromaDB가 비는 문제가 있어, `app/main.py` lifespan에서 스토어가 비어있으면 부팅 시 자동 재적재하도록 수정(실패해도 앱 부팅은 막지 않음). 수동 트리거용 `/legal-qa/ingest-sample-corpus` 엔드포인트도 유지.
- [ ] Render 무료플랜은 15분 미사용 시 슬립 → 재기동 20~30초 지연 있음(비용 들여 Starter 이상으로 올리면 해소, 현재는 감수)

## 8. 직종 커버리지 매트릭스 대조 (2026-08-01, 사용자 제공 매트릭스 기준)
| 직종 | 핵심 포인트 | 상태 |
|---|---|---|
| 제조 | 교대제수당/주52시간 | ✅ overtime_premium(야간) + weekly_hour_limit |
| 제조 | 중대재해/안전보건 연계 | ❌ 범위 밖 확정(사업주 컴플라이언스 중심, 전화응대와 결이 다름) |
| 물류 | 일용·단시간 주휴수당 | ✅ 신규 추가 |
| 물류 | 창고 위장도급 판별 | ✅ 위장도급 계산기(범용, 이미 커버) |
| 사무 | 포괄임금제 적정성 | ✅ 신규 추가 |
| 사무 | 연차휴가 촉진 | ❌ 절차성 규정(날짜 기반 컴플라이언스라 계산기보다 RAG 설명이 적합, 코퍼스 추가는 다음 세션) |
| 사무 | 선택적 근로시간제 | ❌ 미반영 |
| 수행비서/운전기사 | 대기시간·장거리수당 | ⚠️ overtime_premium으로 부분커버(대기시간의 근로시간 해당여부는 RAG 설명 필요, 미반영) |
| 수행비서/운전기사 | 배차일지 교차검증 | ❌ 범위 밖 확정(기록관리 도구, 계산/법령 답변 아님) |
| 프로그래머 | 위장프리랜서 3.3% | ✅ 이미 커버 |
| 프로그래머 | 직무발명보상/저작권 | ❌ 미반영 |
| 서비스직 | 교대근무패턴 | ⚠️ overtime_premium으로 부분커버 |
| 서비스직 | 감정노동자 보호 | ✅ RAG 코퍼스 추가(산업안전보건법 제41조) |
| 감시단속적 | 감단승인요건 | ✅ 이미 커버 |
| 감시단속적 | 야간수당 분리 | ✅ 신규 추가(실무 함정 — 감단승인 있어도 야간수당은 별도 지급) |

## 실행 방법
```bash
# 백엔드 (기본: 인증 비활성)
cd backend && .venv/Scripts/python.exe -m uvicorn app.main:app --port 8010
# 인증 활성화하려면: APP_PASSWORD=원하는비밀번호 <위 명령>

# 프론트엔드
cd frontend && npm run dev -- -p 3010
```
